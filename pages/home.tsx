import React, { ReactElement } from 'react'

interface Props {
    
}

export default function home({}: Props): ReactElement {
    return (
        <div>
            <h1>Home</h1>
        </div>
    )
}
